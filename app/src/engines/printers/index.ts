export type { DetectedPrinter, PrinterSource } from "./types";
// createPrinterSource is deliberately NOT exported here — see that file's
// own comment. Import it directly from "@engines/printers/createPrinterSource".
