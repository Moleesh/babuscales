export type { LicensingSource, LicenseState } from "./types";
// createLicensingSource is deliberately NOT exported here — see that
// file's own comment. Import it directly from
// "@engines/licensing/createLicensingSource" (the provider).
