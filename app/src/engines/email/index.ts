export type { EmailSource, SendTicketEmailInput, SendTicketEmailResult } from "./types";
// createEmailSource is deliberately NOT exported here — see that file's own
// comment. Import it directly from "@engines/email/createEmailSource".
