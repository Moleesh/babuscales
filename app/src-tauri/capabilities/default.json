{
    "$schema": "../gen/schemas/desktop-schema.json",
    "identifier": "default",
    "description": "Capabilities granted to the main window. Extended per module as commands land — see PLAN §6 (store), §16 (devices), §15 (print).",
    "windows": ["main"],
    "permissions": ["core:default"]
}
