//! Secrets and the audit chain. Credentials go to the Windows Credential
//! Manager only — never a config file, never the repository (PLAN §18).
//! Hash-chain verification for the audit table lives here too.
